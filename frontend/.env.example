# Optional public frontend origin used when generating meeting links.
# For local cross-device testing, replace this with the computer's LAN address.
VITE_PUBLIC_APP_URL=https://192.168.1.100:5173

# Override only when the API is hosted somewhere other than the frontend host.
# VITE_API_BASE_URL=https://192.168.1.100:8000
